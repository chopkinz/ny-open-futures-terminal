//! Bar-by-bar backtest: ORB (and later failed breakout, sweep). No lookahead.
//! Entry/stop/target models; expectancy ranking.

// Stub: will consume session ORs + OHLCV, run ORB execution, return TradeRecord list + PerformanceSummary.
